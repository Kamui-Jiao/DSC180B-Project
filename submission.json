{
    "config": ["config/etl.json", "config/model.json"],  # paths of at least 2 config files
    "build": "run.py", # path of your build script
    "library": ["src"], # directory containing (your) library code
    "notebook": ["notebooks/DSC180A_Checkpoint2.ipynb .ipynb"] # path to at least one notebook meant for reading by someone other than you.
    "targets": ["etl", "feature", "model"] # at least two targets
}
